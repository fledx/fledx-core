# fledx CLI Configuration
# Copy this file to .env.cli and adjust the values for your environment
# Or export these variables in your shell profile (~/.bashrc, ~/.zshrc, etc.)

# Control Plane Connection
FLEDX_CLI_CONTROL_PLANE_URL=http://localhost:49421

# Operator Authentication
# Use one of the operator tokens configured in the control plane
FLEDX_OPERATOR_TOKEN=change-me-operator-token-123

# Node Registration (only needed for 'fledx nodes register' command)
FLEDX_REGISTRATION_TOKEN=change-me-registration-token-123

# Optional: Default output format (table, json, yaml)
# FLEDX_OUTPUT_FORMAT=table
